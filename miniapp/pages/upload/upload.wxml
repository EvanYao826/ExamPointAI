<view class="upload-page">
  <!-- Tab 切换 -->
  <view class="tabs">
    <view class="tab {{tab === 'all' ? 'active' : ''}}" bindtap="switchTab" data-tab="all">全部题库</view>
    <view class="tab {{tab === 'done' ? 'active' : ''}}" bindtap="switchTab" data-tab="done">已上传</view>
  </view>

  <!-- 题库列表 -->
  <view class="task-list">
    <view class="task-card" wx:for="{{tasks}}" wx:key="id">
      <view class="task-top">
        <text class="task-name">{{item.bank_name || '未知题库'}}</text>
        <view class="status-badge status-{{item.status}}">
          {{item.status === 0 ? '已上传' : item.status === 1 ? '解析中' : item.status === 2 ? '解析成功' : '解析失败'}}
        </view>
      </view>
      <view class="task-bottom">
        <text class="task-info">{{item.success_count || 0}} 题成功 · {{item.fail_count || 0}} 题失败</text>
        <text class="task-time">{{item.create_time}}</text>
      </view>
    </view>

    <!-- 空状态 -->
    <view class="empty" wx:if="{{tasks.length === 0 && !loading}}">
      <text class="empty-icon">📭</text>
      <text class="empty-text">暂无题库，点击下方按钮上传</text>
    </view>
  </view>

  <!-- 上传按钮 -->
  <view class="btn-upload" hover-class="btn-upload-hover" bindtap="onUpload">
    <text class="btn-upload-icon">+</text>
    <text class="btn-upload-text">上传题库</text>
  </view>
</view>
